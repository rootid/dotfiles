brewfile.lock.json
packages/vim/.vim/pack/others/start/
packages/emacs/emacs_config/basic.el
*~
bkup
result
tmp/
tmp.*

# Kept in sync with packages/git/.gitignore (the global core.excludesfile) so
# this repo stays clean even before `make link_config_files` has run.
.DS_Store
*.swp
*.swo
*.out
*.log

# AI agent credentials and runtime state that live next to the config we do
# track in packages/{claude,gemini,agents}. Never commit these.
packages/gemini/.gemini/oauth_creds.json
packages/gemini/.gemini/google_accounts.json
packages/gemini/.gemini/installation_id
packages/claude/.claude/settings.local.json
packages/claude/.claude/.credentials.json

fixme.md
